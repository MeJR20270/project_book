<!DOCTYPE html>
<html>
<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <title>จัดการคำขอบริจาค</title>
</head>
<body>
<%- include('partials/navbar') %>
<div class="container mt-4">
  <h2>จัดการคำขอบริจาค</h2>
  <table class="table table-bordered table-striped">
    <thead class="table-dark">
      <tr>
        <th>ผู้ใช้</th>
        <th>หนังสือ</th>
        <th>สถานะ</th>
        <th>เปลี่ยนสถานะ</th>
      </tr>
    </thead>
    <tbody>
      <% if (requests.length === 0) { %>
        <tr>
          <td colspan="4" class="text-center">ยังไม่มีคำขอบริจาค</td>
        </tr>
      <% } else { %>
        <% requests.forEach(r => { %>
          <tr>
            <td><%= r.username %></td>
            <td><%= r.title %></td>
            <td><%= r.status %></td>
            <td>
              <form method="POST" action="/admin/update-status" class="d-flex">
                <input type="hidden" name="request_id" value="<%= r.id %>">
                <select name="status" class="form-select me-2">
                  <option value="กำลังดำเนินการ" <%= r.status==='กำลังดำเนินการ'?'selected':'' %>>กำลังดำเนินการ</option>
                  <option value="ขณะจัดส่ง" <%= r.status==='ขณะจัดส่ง'?'selected':'' %>>ขณะจัดส่ง</option>
                  <option value="จัดส่งสำเร็จ" <%= r.status==='จัดส่งสำเร็จ'?'selected':'' %>>จัดส่งสำเร็จ</option>
                </select>
                <button class="btn btn-primary">อัปเดต</button>
              </form>
            </td>
          </tr>
        <% }) %>
      <% } %>
    </tbody>
  </table>
</div>
</body>
</html>
